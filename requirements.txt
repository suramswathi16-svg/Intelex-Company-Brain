fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

sentence-transformers==3.1.1
transformers==4.44.2
torch==2.9.0

faiss-cpu==1.8.0
networkx==3.3
pypdf==4.3.1

langchain==0.3.1          # optional, only used for RecursiveCharacterTextSplitter
                            # (a built-in fallback splitter is used automatically
                            #  if this package is not installed)

# Optional: uncomment if you want a real Neo4j graph instead of the
# built-in in-memory NetworkX graph (also set env var USE_NEO4J=true)
# neo4j==5.24.0

# Optional: uncomment + adapt OrgMemoryStore to use PostgreSQL instead of SQLite
# psycopg2-binary==2.9.9
# sqlalchemy==2.0.35
